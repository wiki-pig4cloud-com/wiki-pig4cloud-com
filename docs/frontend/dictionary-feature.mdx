---
title: "前端字典功能"
---

## useDict 函数

<Note>
字典类型对应 `sys_dict` 表中字典数据。
</Note>

```typescript
import { useDict } from '/@/hooks/dict';

const { 字典类型A, 字典类型B } = useDict('字典类型 A', '字典类型 B');
```

## 页面使用 dict-tag

```html
<el-table-column>
  <template #default="scope">
    <dict-tag :options="字典类型" :value="scope.row.xx"></dict-tag>
  </template>
</el-table-column>
```

## ♥️ 获取支持
<Card title="遇到问题？" horizontal icon="link" href="https://gitee.com/log4j/pig/issues">
  如果您在使用过程中遇到任何问题、有功能建议或需求，请点击此卡片前往 Gitee 仓库提交 Issue。
</Card>

